{%- if include.inline != "true" %}
在本章節，你將把自製韌體安裝到主機。
{%- endif %}

1. 當出現提示時，輸入上方螢幕出現的按鍵組合來安裝 boot9strap
    + 如果任何步驟在下方螢幕出現紅色文字，且未出現按鍵組合提示，[跟著除錯步驟做](troubleshooting#issues-with-safeb9sinstaller)
{%- if include.isbootfirm == "true" %}
1. 完成後，長壓電源鈕以強制關機
    + 下個章節完成前，你的主機只會啟動至 SafeB9SInstaller 畫面
{%- else %}
1. 完成後, 按 (A) 重新啟動主機
{%- endif %}
